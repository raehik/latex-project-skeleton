%
% General typography including margins, vertical spacing etc.
%

% change margins: defaults are recommended, but 1.25in is good for saving paper
%\usepackage[margin=1.25in]{geometry}

% use only 1 space at end of sentences, instead of default 2
% The subject of many debates: up to you, I don't change from the default.
%\frenchspacing

% add vertical spacing between paragraphs & remove indent
% Basically a "make it Microsoft Word" package.
%\usepackage{parskip}
% For parskip: keep the default 15pt indent (so just add vertical spacing)
%\setlength{\parindent}{15pt}

% specify default font style
%   \rmdefault : serif (roman) (default)
%   \sfdefault : sans-serif
%   \ttdefault : monospace (typewriter)
%\renewcommand{\familydefault}{\rmdefault}
