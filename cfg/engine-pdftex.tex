%
% pdfTeX-specific config.
%

% use Latin Modern font, successor to (better than) Computer Modern/cm-super
\usepackage{lmodern}

% use T1 8-bit encoding e.g. accented characters are distinct glyphs
\usepackage[T1]{fontenc}

% allows UTF-8 input e.g. input accented characters directly rather than \"a
\usepackage[utf8]{inputenc}
