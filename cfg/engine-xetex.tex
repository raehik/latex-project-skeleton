%
% XeTeX-specific config.
%

% fonts via fontspec (unified font selection for XeTeX, LuaTeX)
\usepackage{fontspec}
\setmainfont{Latin Modern Roman}

% more TeX logo macros e.g. \XeTeX, \LuaTeX, and optimised for more fonts
\usepackage{metalogo}

% TODO
% Make sure TeX ligatures (-- -> long dash, ` -> left quote, etc.).
% Note: In my experiments, this doesn't change anything. Can't tell if it's
%       default or not.
%\defaultfontfeatures{Ligatures=TeX}

% TODO CJK: https://tex.stackexchange.com/q/17611/87363
