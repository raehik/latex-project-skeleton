%
% Support for adding images 
%
% Supports both LaTeX and XeLaTeX. Uses the specific preamble for the compiler
% (e.g. preamble-xelatex for xelatex) with the ifxetex package.
%

% TODO: again, make img a variable...
\usepackage{graphicx}
\graphicspath{ {img/} }
\usepackage{float}
