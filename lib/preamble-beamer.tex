%
% Beamer-specific preamble.
%
% Same as the generic preamble, supports both LaTeX and XeLaTeX (I think).
%

\documentclass{beamer}

\addmylib{fonts}   % fonts
\addmylib{spacing} % margins, parskip
\addmylib{code}    % listings, syntax highlighting
\addmylib{images}  % images

% Beamer theme {{{
% main theme
%\usetheme{Rochester}

% disable bottom nav bar
%\setbeamertemplate{navigation symbols}{}
% }}}

%% Optional {{{
% lorem ipsum for placeholders & formatting testing
%\usepackage{lipsum}
%% }}}

% (hyperref must be disabled for beamer)

% specific preamble depending on TeX compiler
\usepackage{ifxetex}
\ifxetex
    \addmylib{preamble-xelatex}
\else
    \addmylib{preamble-latex}
\fi

